name: Continuous Integration

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  integration-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Create Dummy .env for Prisma
      run: |
        # Create a .env file in the backend directory so Prisma can read it during the build
        echo 'DATABASE_URL="postgresql://dummy:dummy@localhost:5432/dummy"' > backend/.env

    - name: Start Full Stack with Docker Compose
      run: |
        # Boot up the database, backend, and frontend in the background
        docker compose up -d --build
        
        # Wait a moment for Prisma to push the schema and the Node server to start
        echo "Waiting 15 seconds for containers to initialize..."
        sleep 15
        
        # Check if the containers are actually running or if they crashed
        docker ps
        docker compose logs backend

    - name: Fix File Permissions
      run: |
        # Give ownership of the files created by Docker back to the GitHub runner user
        sudo chown -R $USER:$USER .

    - name: Install Test Dependencies
      run: |
        cd backend
        npm install ts-node typescript @types/node

    - name: Run API Integration Test
      run: |
        cd backend
        # This will call the API which will fetch from the real Postgres database
        # npm run test:ci

    - name: Shut down Docker
      if: always() # Run this even if the tests fail
      run: docker compose down
